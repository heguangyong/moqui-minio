<?xml version="1.0" encoding="UTF-8"?>
<screen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/xml-screen-3.xsd">
    <widgets>
        <form-single name="BucketForm">
            <!-- Common fields for create and edit -->
            <field name="bucketName">
                <default-field title="${ec.l10n.localize('Bucket Name')}">
                    <text-line size="30" required="true"/>
                </default-field>
            </field>
            <field name="description">
                <default-field title="${ec.l10n.localize('Description')}">
                    <text-area rows="3" cols="50"/>
                </default-field>
            </field>
            <field name="quotaLimit">
                <default-field title="${ec.l10n.localize('Quota Limit (Bytes)')}">
                    <text-line size="20"/>
                </default-field>
            </field>

            <!-- Fields only for creation -->
            <field-group name="CreateOnlyFields" condition="isCreate == 'true'">
                <field name="bucketId">
                    <default-field title="${ec.l10n.localize('Bucket ID')}">
                        <text-line size="30" required="true"/>
                    </default-field>
                </field>
                <field name="isPublic">
                    <default-field title="${ec.l10n.localize('Public Access')}">
                        <drop-down>
                            <option key="N" text="${ec.l10n.localize('Private')}"/>
                            <option key="Y" text="${ec.l10n.localize('Public')}"/>
                        </drop-down>
                    </default-field>
                </field>
                <field name="versioning">
                    <default-field title="${ec.l10n.localize('Versioning')}">
                        <drop-down>
                            <option key="N" text="${ec.l10n.localize('Disabled')}"/>
                            <option key="Y" text="${ec.l10n.localize('Enabled')}"/>
                        </drop-down>
                    </default-field>
                </field>
                <field name="encryption">
                    <default-field title="${ec.l10n.localize('Encryption')}">
                        <drop-down>
                            <option key="N" text="${ec.l10n.localize('Disabled')}"/>
                            <option key="Y" text="${ec.l10n.localize('Enabled')}"/>
                        </drop-down>
                    </default-field>
                </field>
            </field-group>

            <!-- Hidden fields -->
            <field name="bucketId" condition="isCreate != 'true'">
                <default-field><hidden/></default-field>
            </field>
            <field name="userId">
                <default-field><hidden/></default-field>
            </field>
        </form-single>
    </widgets>
</screen>
