<?xml version="1.0" encoding="UTF-8"?>
<screen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/xml-screen-3.xsd"
        default-menu-title="${ec.l10n.localize('Bucket Management')}"
        default-menu-index="10"
        default-menu-include="true"
        require-authentication="true">

    <transition name="createBucket">
        <service-call name="minio.MinioServices.create#Bucket"/>
        <default-response url="."/>
    </transition>
    <transition name="updateBucket">
        <service-call name="minio.MinioServices.update#Bucket"/>
        <default-response url="."/>
    </transition>
    <transition name="deleteBucket">
        <service-call name="minio.MinioServices.delete#Bucket"/>
        <default-response url="."/>
    </transition>
    <transition name="fileExplorer">
        <default-response url="../Bucket/FileExplorer/ElFinder">
            <parameter name="bucketName"/>
            <parameter name="resourceRoot"/>
        </default-response>
    </transition>

    <actions>
        <set field="userId" from="ec.user.userId"/>
        <set field="isAdmin" from="ec.user.isInGroup('ADMIN') || ec.user.isInGroup('ADMIN_ADV')"/>
        <set field="scope" from="scope ?: 'user'"/>
        <service-call name="minio.MinioServices.list#Bucket"
                      in-map="[userId:userId, scope:scope, pageIndex:pageIndex, pageSize:pageSize]"
                      out-map="context"/>
    </actions>

    <widgets>
        <container-box id="bucket-management-box">
            <box-header>
                <label text="${ec.l10n.localize('Bucket Management')}" type="h2"/>
            </box-header>

            <!-- 创建桶对话框 -->
            <box-toolbar>
                <container-dialog id="CreateBucketDialog" button-text="${ec.l10n.localize('Create Bucket')}" type="primary">
                    <form-single name="CreateBucketForm" transition="createBucket" focus-field="bucketId">
                        <field name="bucketId"><default-field title="${ec.l10n.localize('Bucket ID')}"><text-line size="20"/></default-field></field>
                        <field name="bucketName"><default-field title="${ec.l10n.localize('Bucket Name')}"><text-line size="20"/></default-field></field>
                        <field name="userId"><default-field><hidden/></default-field></field>
                        <field name="description"><default-field title="${ec.l10n.localize('Description')}"><text-area rows="3" cols="50"/></default-field></field>
                        <field name="quotaLimit"><default-field title="${ec.l10n.localize('Quota Limit (Bytes)')}"><text-line size="15" default-value="5368709120"/></default-field></field>
                        <field name="isPublic"><default-field title="${ec.l10n.localize('Public Access')}">
                            <drop-down>
                                <option key="N" text="${ec.l10n.localize('Private')}"/>
                                <option key="Y" text="${ec.l10n.localize('Public')}"/>
                            </drop-down>
                        </default-field></field>
                        <field name="versioning"><default-field title="${ec.l10n.localize('Versioning')}">
                            <drop-down>
                                <option key="N" text="${ec.l10n.localize('Disabled')}"/>
                                <option key="Y" text="${ec.l10n.localize('Enabled')}"/>
                            </drop-down>
                        </default-field></field>
                        <field name="encryption"><default-field title="${ec.l10n.localize('Encryption')}">
                            <drop-down>
                                <option key="N" text="${ec.l10n.localize('Disabled')}"/>
                                <option key="Y" text="${ec.l10n.localize('Enabled')}"/>
                            </drop-down>
                        </default-field></field>
                        <field name="submitButton"><default-field title="${ec.l10n.localize('Save')}"><submit/></default-field></field>
                    </form-single>
                </container-dialog>

                <!-- 管理员查看选项 -->
                <section name="adminView" condition="isAdmin">
                    <widgets>
                        <container>
                            <link url="." text="${ec.l10n.localize('查看我的网盘')}" parameter-map="[scope:'user']" condition="scope == 'all'"/>
                            <link url="." text="${ec.l10n.localize('查看所有网盘')}" parameter-map="[scope:'all']" condition="scope != 'all'"/>
                        </container>
                    </widgets>
                </section>
            </box-toolbar>

            <!-- 桶列表 -->
            <box-body>
                <form-list name="ListBuckets" list="bucketList"  paginate="true"  skip-form="true">
                    <field name="bucketId">
                        <header-field title="${ec.l10n.localize('Bucket ID')}"/>
                        <default-field>
                            <link text="${bucketId}" url="fileExplorer" parameter-map="[bucketName:bucketName]"/>
                        </default-field>
                    </field>
                    <field name="bucketName">
                        <header-field title="${ec.l10n.localize('Bucket Name')}"/>
                        <default-field><display/></default-field>
                    </field>
                    <field name="quotaLimit">
                        <header-field title="${ec.l10n.localize('Quota Limit (Bytes)')}"/>
                        <default-field><display/></default-field>
                    </field>
                    <field name="createdDate">
                        <header-field title="${ec.l10n.localize('Created Date')}"/>
                        <default-field><display format="yyyy-MM-dd HH:mm:ss"/></default-field>
                    </field>
                    <field name="actions">
                        <header-field title="${ec.l10n.localize('Actions')}"/>
                        <default-field>
                            <link url="deleteBucket" text="${ec.l10n.localize('Delete')}" confirmation="确定要删除这个网盘吗？删除后将无法恢复。">
                                    <parameter name="bucketId" from="bucketId"/>
                                </link>
                                <container-dialog id="EditBucketDialog" button-text="${ec.l10n.localize('Edit')}">
                                    <form-single name="EditBucketForm" transition="updateBucket">
                                        <field name="bucketId"><default-field><hidden/></default-field></field>
                                        <field name="userId"><default-field><hidden/></default-field></field>
                                        <field name="bucketName"><default-field title="${ec.l10n.localize('Bucket Name')}"><text-line size="20"/></default-field></field>
                                        <field name="description"><default-field title="${ec.l10n.localize('Description')}"><text-area rows="3" cols="50"/></default-field></field>
                                        <field name="quotaLimit"><default-field title="${ec.l10n.localize('Quota Limit (Bytes)')}"><text-line size="15"/></default-field></field>
                                        <field name="submitButton"><default-field title="${ec.l10n.localize('Update')}"><submit/></default-field></field>
                                    </form-single>
                                </container-dialog>
                        </default-field>
                    </field>
                </form-list>
            </box-body>

        </container-box>
    </widgets>
</screen>
